{
  "env": {
    "PRISMA_CLI_BINARY_TARGETS": "rhel-openssl-3.0.x"
  },
  "buildCommand": "npm run build",
  "functions": {
    "app/api/**/*.ts": {
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/$1"
    }
  ]
}